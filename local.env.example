# AWS Configuration (Replace with your actual credentials)
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=your_aws_region_here

# Cognito Configuration (Replace with your actual values)
COGNITO_USER_POOL_ID=your_cognito_user_pool_id_here
COGNITO_CLIENT_ID=your_cognito_client_id_here

# Database Configuration (Replace with your actual database connection)
CONNECTION_STRING=Host=your_host;Port=5432;Database=your_db;Username=your_user;Password=your_password;SSL Mode=Prefer;Trust Server Certificate=true

# JWT Configuration (Generate a secure random key for production)
JWT_KEY=your_very_long_secret_key_for_jwt_signing_that_is_at_least_64_characters_long_for_security

# Instructions:
# 1. Copy this file to 'local.env' (without .example)
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit the local.env file to git
# 4. Keep your credentials secure and never share them
